# Database Configuration
DATABASE_URL=postgresql://hudi_user:password@localhost/hudi_bootstrap_db

# Hive Configuration
HIVE_HOST=localhost
HIVE_PORT=10000

# Spark Configuration (optional)
SPARK_MASTER=local
SPARK_CONF_KEY=value  # Replace with actual Spark conf key-value pairs if needed

# HDFS Configuration
HDFS_HOST=hdfs://localhost:9000  # Example HDFS URL

